[package]
name = "midijson"
version = "0.1.0"
edition = "2024"

[dependencies]
from-to-repr = { version = "0.2.2", features = ["from_to_other"] }
serde = { version = "1.0.219", features = ["derive"] }
